# Usage
To compile:
make fast        // Basic
make debug       // Debug mode, extra prints
make debug-clang // Debug mode, extra prints, sanitizer
(Then run `./main.out`)

make profile     // Uses gprof to profile the program
`./main.out`
`gprof main.out gmon.out > analysis.txt`

# TODO

## Fixes
1. Need to play around with the code to find out more issues

## Enchancements
1. Add more plant subclasses
2. Add plant name enum (plant name string comparison is really really slow)
3. Add plant refresh timer (using enum)
4. Add random zombie generator?
5. Figure out swig
